html
    head
        title My Site
        link(rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css")
    body
        nav.navbar.navbar-light.bg-light
            a.navbar-brand(style="text-align:center" href='#') Internet Of Thing
        .container.pt-5
            h1 Login
            if errors
                each error in errors
                    .alert.alert-danger= error
            form(method='POST')
                .form-group
                    label(for="exampleInputEmail1") Email
                    input.form-control(name="email" type="text" placeholder="Email" aria-label="Recipient's username" aria-describedby="basic-addon2")
                .form-group       
                    label(for="exampleInputEmail1") Pass
                    input.form-control(name= "pass" type="text" placeholder="pass")
                button.btn.btn-primary Login